import React from 'react';

const RouteStopsLocationDetailModal = (props) =>
  <div>
    MVP+
    should be able to post comments 
    aka TODO's  //will need to add another property in the route object with array of text
  </div>;

export default RouteStopsLocationDetailModal;